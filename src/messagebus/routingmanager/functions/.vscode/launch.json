{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Routing Manager",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}