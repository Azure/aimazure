<#
.SYNOPSIS
Tears down the the topic publisher logic app.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
.\TearDown-95-TopicPublisher-LogicApp.ps1
#>

& $PSScriptRoot\Remove-TopicPublisher-LogicApp.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -workflowName "{{ resource_template.resource_name }}"