<#
.SYNOPSIS
Invokes the teardown of a message suspend processors service bus api connection.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
.\TearDown-80-SystemApplicationServiceBus-ApiConnection.ps1
#>

& $PSScriptRoot\Remove-SystemApplicationServiceBus-ApiConnection.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -resourceName "{{ resource_template.resource_name }}"