<#
.SYNOPSIS
Invokes the teardown of a topic subscription resource.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
./TearDown-30-TopicChannelSubscription-ServiceBus.ps1
#>

& $PSScriptRoot\Remove-TopicChannelSubscription-ServiceBus.ps1 -resourceGroupName "{{ resource_template.parameters.azure_resource_group_name }}" -resourceName "{{ resource_template.resource_name }}" -serviceBusNamespace "{{ resource_template.parameters.service_bus_namespace }}" -serviceBusTopic "{{ resource_template.parameters.topic_name }}"
