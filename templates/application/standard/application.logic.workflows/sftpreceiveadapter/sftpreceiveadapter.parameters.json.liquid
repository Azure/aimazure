{
	"{{ resource_template.parameters.workflow_name }}.sftpReceiveConnection.apiId": {
		"type": "String",
		"value": "/subscriptions/@appsetting('apiConnectionSubscriptionId')/providers/Microsoft.Web/locations/@appsetting('apiConnectionLocation')/managedApis/sftpwithssh"
	},
	"{{ resource_template.parameters.workflow_name }}.sftpReceiveConnection.resourceId": {
		"type": "String",
		"value": "/subscriptions/@appsetting('apiConnectionSubscriptionId')/resourceGroups/@appsetting('apiConnectionResourceGroupName')/providers/Microsoft.Web/connections/@appsetting('{{ resource_template.parameters.workflow_name }}.sftpReceiveConnection.name')"
	},
	"{{ resource_template.parameters.workflow_name }}.sftpReceiveConnection.apiAuthentication": {
		"type": "Object",
		"value": {
			"type": "ManagedServiceIdentity"
		}
	},
	"{{ resource_template.parameters.workflow_name }}.sftpReceiveAdapter.scenarioName": {
		"value": "{{ resource_template.parameters.scenario_name }}",
		"description": "The name of the scenario for this specific Receive Adapter.",
		"type": "String"
	},
	"{{ resource_template.parameters.workflow_name }}.sftpReceiveAdapter.sftpReceiveFolder": {
		"value": "{{ resource_template.parameters.azure_sftp_target_folder }}",
		"description": "The path where the SFTP connector looks for new/updated files.",
		"type": "String"
	},
	"{{ resource_template.parameters.workflow_name }}.sftpReceiveAdapter.fileNameMask": {
		"value": "{{ resource_template.parameters.azure_sftp_file_mask }}",
		"description": "The file mask to use when checking for files.",
		"type": "String"
	},
	"{{ resource_template.parameters.workflow_name }}.sftpReceiveAdapter.sftpMaxFileCount": {
		"value": {{ resource_template.parameters.azure_sftp_max_file_count }},
		"description": "The max number of files the SFTP Connector will retrieve per polling interval.",
		"type": "Int"
	},
	"{{ resource_template.parameters.workflow_name }}.sftpReceiveAdapter.sftpRecurrenceFrequency": {
		"value": "{{ resource_template.parameters.azure_sftp_frequency }}",
		"description": "The frequency with which to check for new files on the SFTP server.",
		"type": "String"
	},
	"{{ resource_template.parameters.workflow_name }}.sftpReceiveAdapter.sftpRecurrenceInterval": {
		"value": {{ resource_template.parameters.azure_ftp_recurrence }},
		"description": "The interval with which to check for new files on the SFTP server.",
		"type": "Int"
	}
}