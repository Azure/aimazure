<#
.SYNOPSIS
Clears the APIM cache for all workflows in this application Logic App

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
.\Deploy-120-Application-ClearUrlCache.ps
#>

& $PSScriptRoot\New-Application-ClearUrlCache.ps1 --resourceGroupName "{{ resource_template.parameters.azure_resource_group_name }}" -resourceName "{{ resource_template.resource_name }}" -workflowName "workflow" -apimInstanceName "{{ resource_template.parameters.apim_instance_name}}" -keyVaultName "{{ resource_template.parameters.key_vault_name}}" -keyVaultApimSubscriptionSecretName "{{ resource_template.parameters.key_vault_apim_subscription_secret_name}}"
