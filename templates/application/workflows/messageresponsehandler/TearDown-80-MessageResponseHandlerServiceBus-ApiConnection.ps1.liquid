<#
.SYNOPSIS
Invokes the teardown of a message response handlers service bus api connection.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
.\TearDown-80-MessageResponseHandlerServiceBus-ApiConnection.ps1
#>

& $PSScriptRoot\Remove-MessageResponseHandlerServiceBus-ApiConnection.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -resourceName "{{ resource_template.resource_name }}"