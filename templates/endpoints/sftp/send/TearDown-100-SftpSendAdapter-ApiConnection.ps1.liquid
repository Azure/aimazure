<#
.SYNOPSIS
Invokes the teardown of sftp connector.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
./TearDown-100-SftpSendAdapter-ApiConnection.ps1
#>

& $PSScriptRoot\Remove-SftpSendAdapter-ApiConnection.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -resourceName "{{ resource_template.resource_name }}"