<#
.SYNOPSIS
Invokes the teardown of sftp adapter.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
./TearDown-105-SftpSendAdapter-LogicApp.ps1
#>

& $PSScriptRoot\Remove-SftpSendAdapter-LogicApp.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -resourceName "{{ resource_template.resource_name }}"