<#
.SYNOPSIS
Invokes the teardown of a file receive adapters ile system api connection.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
.\TearDown-100-FileReceiveAdapterFileSystem-ApiConnection.ps1
#>

& $PSScriptRoot\Remove-FileReceiveAdapterFileSystem-ApiConnection.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -resourceName "{{ resource_template.resource_name }}"
