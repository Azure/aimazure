<#
.SYNOPSIS
Invokes the teardown of a Map from store.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
TearDown-120-Map.ps1
#>

& $PSScriptRoot\Remove-Map.ps1 -mapFile "\\?\$PSScriptRoot\{{ resource_template.parameters.azure_message_map_name }}.xslt" -resourceGroupName "{{ resource_template.parameters.azure_resource_group_name }}" -integrationAccountName "{{ resource_template.parameters.azure_integration_account_name }}"