<#
.SYNOPSIS
Invokes the teardown of the API resource in API Management for a configuration manager.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
./TearDown-40-ConfigManager-ApiManagement.ps1
#>

& $PSScriptRoot\Remove-ConfigManager-ApiManagement.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -serviceName "{{ resource_template.resource_name }}" -apiName "{{ resource_template.parameters.api_name }}"
