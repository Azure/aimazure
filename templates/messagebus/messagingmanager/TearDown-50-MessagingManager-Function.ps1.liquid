<#
.SYNOPSIS
Invokes the teardown of the Function resource.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
./TearDown-50-MessagingManager-Function.ps1
#>

& $PSScriptRoot\Remove-MessagingManager-Function.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -resourceName "{{ resource_template.resource_name }}"