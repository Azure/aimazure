<#
.SYNOPSIS
Invokes the teardown of the API resource in API Management for a messaging manager.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
./TearDown-60-MessagingManager-ApiManagement.ps1
#>

& $PSScriptRoot\Remove-MessagingManager-ApiManagement.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -serviceName "{{ resource_template.resource_name }}" -apiName "{{ resource_template.parameters.api_name }}"
