<#
.SYNOPSIS
Invokes the deployment of a custom Role resource for use by API Management service.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
./Deploy-5-MessageBusService-Role.ps1
#>

& $PSScriptRoot\New-MessageBusService-Role.ps1 -subscriptionId "{{ resource_template.parameters.azure_subscription_id }}" -role "{{ resource_template.parameters.role_name }}"
