<#
.SYNOPSIS
Invokes the teardown of the event grid subscribe api connection.

.DESCRIPTION
Prior to running this script ensure you are authenticated against Azure and have the desired subscription set.

.EXAMPLE
.\TearDown-80-EventGridSubscribe-ApiConnection.ps1
#>

& $PSScriptRoot\Remove-EventGridSubscribe-ApiConnection.ps1 -resourceGroup "{{ resource_template.parameters.azure_resource_group_name }}" -resourceName "{{ resource_template.resource_name }}"